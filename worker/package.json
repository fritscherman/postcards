{
  "name": "postcards-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create postcards",
    "db:migrate": "wrangler d1 migrations apply postcards --remote",
    "db:migrate:local": "wrangler d1 migrations apply postcards --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "wrangler": "^3.114.1"
  }
}
